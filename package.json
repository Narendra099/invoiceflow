{
  "name": "invoiceflow",
  "version": "1.0.0",
  "description": "Full-stack invoicing app — React + Node + TypeScript. Single-service deployable.",
  "private": true,
  "scripts": {
    "build": "npm --prefix client install --include=dev && npm --prefix client run build && npm --prefix server install --include=dev && npm --prefix server run build",
    "start": "npm --prefix server start",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "engines": {
    "node": "22.x"
  }
}
